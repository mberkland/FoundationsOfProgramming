// Arrays
// - More than containers of data
// - They are smart and do things

var myArray = [10, 20, "test", true, 99];

alert(myArray.length);

// methods are functions that belong to an object
// call using name of object, "dot", then the name of the 
// method, then paranthesis

// someObject.someMethod(); -  e.g. to call a method

var myNewArray = [10, "a", 1, 40, 50];

myNewArray.reverse();

// important array methods to know
// .sort(), .join(), .pop(), .push()

// not supposed to memorize  all the methods
// use mozzila JavaScript reference guide
